interface:
  display_name: "cc:setup"
  short_description: "Args: --enable-review-gate, --disable-review-gate. Verify Claude Code setup or toggle the review gate."
policy:
  allow_implicit_invocation: false
