interface:
  display_name: "cc:run"
  short_description: "Args: --wait, --background, --write, --model <model>, --effort <low|medium|high|max>, --prompt-file <path>, [task text]. Run one Claude Code request."
